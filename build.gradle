plugins {
    id "org.sonarqube" version "3.3"
}

sonarqube {
    properties {
        property "sonar.projectKey", "artrayme_WINK-back"
        property "sonar.organization", "artrayme"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}